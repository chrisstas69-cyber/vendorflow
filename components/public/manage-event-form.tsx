'use client';
import { FormEvent, useState } from 'react';
export function ManageEventForm({ token, event }: { token: string; event: { name: string; description: string; vendorApplicationUrl: string; vendorDeadline: string; boothFee: number | '' } }) { const [message, setMessage] = useState(''); async function save(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const body = Object.fromEntries(new FormData(e.currentTarget)); const res = await fetch(`/api/events/manage/${token}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) }); const json = await res.json(); setMessage(json.message ?? json.error); } const input='mt-1 w-full rounded-lg border vf-border px-3 py-2.5 text-sm'; return <form onSubmit={save} className="mt-7 space-y-4 rounded-2xl border vf-border vf-surface p-6"><label className="block text-sm vf-text-muted">Event name<input name="name" defaultValue={event.name} required className={input} /></label><label className="block text-sm vf-text-muted">Description<textarea name="description" defaultValue={event.description} required rows={5} className={input} /></label><label className="block text-sm vf-text-muted">Vendor application URL<input name="vendorApplicationUrl" type="url" defaultValue={event.vendorApplicationUrl} className={input} /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block text-sm vf-text-muted">Vendor deadline<input name="vendorDeadline" type="date" defaultValue={event.vendorDeadline} className={input} /></label><label className="block text-sm vf-text-muted">Booth fee ($)<input name="boothFee" type="number" min="0" defaultValue={event.boothFee} className={input} /></label></div>{message && <p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-900">{message}</p>}<button className="rounded-lg bg-orange-600 px-5 py-3 font-semibold text-white">Save updates</button></form>; }
